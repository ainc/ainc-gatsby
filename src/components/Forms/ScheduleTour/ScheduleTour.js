import React from "react";
import ModalCustom from "../../Modal/ModalCustom";

const ScheduleTour = ({ lgShow, hide }) => {
  return (
    <ModalCustom lgShow={lgShow} hide={hide}>
      <h1>Schedule</h1>
    </ModalCustom>
  );
};

export default ScheduleTour;
